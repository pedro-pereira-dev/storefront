---

---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>Storefront Launchpad</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600&display=swap"
      rel="stylesheet"
    />
    <style>
      :root {
        color-scheme: light;
        --bg: radial-gradient(circle at top, #fef6e4, #fcf1ff 35%, #eff7ff);
        --card: rgba(255, 255, 255, 0.85);
        --text: #1f1b2c;
        --accent: #ff7b54;
        --accent-dark: #ff5c3a;
      }

      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }

      body {
        min-height: 100vh;
        font-family: "Space Grotesk", "Helvetica Neue", sans-serif;
        background: var(--bg);
        color: var(--text);
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 2rem;
      }

      main {
        width: 100%;
        max-width: 720px;
        background: var(--card);
        border-radius: 28px;
        padding: 3rem;
        box-shadow: 0 25px 55px rgba(31, 27, 44, 0.15);
        backdrop-filter: blur(10px);
      }

      header {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
        margin-bottom: 2rem;
      }

      .badge {
        font-size: 0.85rem;
        text-transform: uppercase;
        letter-spacing: 0.2em;
        color: var(--accent);
      }

      h1 {
        font-size: clamp(2.5rem, 6vw, 3.8rem);
        line-height: 1.1;
      }

      p {
        font-size: 1.1rem;
        line-height: 1.6;
        color: rgba(31, 27, 44, 0.8);
      }

      .actions {
        display: flex;
        flex-wrap: wrap;
        gap: 1rem;
        margin-top: 2.5rem;
      }

      button,
      a.cta-link {
        border: none;
        padding: 0.95rem 1.75rem;
        border-radius: 999px;
        font-weight: 600;
        font-size: 1rem;
        cursor: pointer;
        transition:
          transform 200ms ease,
          box-shadow 200ms ease,
          background 200ms ease;
      }

      .primary {
        background: var(--accent);
        color: white;
        box-shadow: 0 15px 30px rgba(255, 91, 58, 0.35);
      }

      .primary:hover {
        transform: translateY(-2px);
        background: var(--accent-dark);
      }

      .cta-link {
        background: transparent;
        color: var(--text);
        text-decoration: none;
        border: 1px solid rgba(31, 27, 44, 0.2);
      }

      .cta-link:hover {
        transform: translateY(-2px);
      }

      .hero-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
        gap: 1.5rem;
        margin-top: 2.5rem;
      }

      .card {
        background: rgba(255, 255, 255, 0.8);
        border-radius: 20px;
        padding: 1.25rem;
        border: 1px solid rgba(31, 27, 44, 0.08);
        box-shadow: 0 8px 20px rgba(31, 27, 44, 0.08);
      }

      .card span {
        display: block;
        font-size: 0.85rem;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        color: rgba(31, 27, 44, 0.6);
        margin-bottom: 0.4rem;
      }

      .card strong {
        font-size: 1.4rem;
        color: var(--text);
      }

      @media (max-width: 600px) {
        body {
          padding: 1.5rem;
        }

        main {
          padding: 2rem;
        }

        .actions {
          flex-direction: column;
        }
      }
    </style>
  </head>
  <body>
    <main>
      <header>
        <p class="badge">Storefront · Turborepo</p>
        <h1>Launch faster with a focused Astro storefront.</h1>
        <p>
          This single-page experience ships from a pnpm + Turborepo workspace, already wired with
          Prettier formatting and Oxlint checks. Extend the platform with new apps and packages as
          your commerce stack evolves.
        </p>
      </header>
      <div class="actions">
        <button class="primary">Start Building</button>
        <a class="cta-link" href="https://docs.astro.build" target="_blank">Explore Docs</a>
      </div>
      <section class="hero-grid">
        <div class="card">
          <span>Tooling</span>
          <strong>pnpm + Turbo</strong>
        </div>
        <div class="card">
          <span>Quality</span>
          <strong>Prettier & Oxlint</strong>
        </div>
        <div class="card">
          <span>Framework</span>
          <strong>Astro 5</strong>
        </div>
      </section>
    </main>
  </body>
</html>
