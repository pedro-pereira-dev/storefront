---
const { leftLabel, centerLabel, rightLabel } = Astro.props;
---

<header id="site-header" class="sticky top-4 z-50 sm:top-5 lg:top-6">
  <div class="px-4 sm:px-6">
    <nav class="relative mx-auto flex max-w-4xl items-center justify-between gap-3 rounded-[1rem] border border-white/15 bg-white/5/80 px-4 py-4 text-[10px] uppercase tracking-[0.2em] text-white/80 backdrop-blur-xl backdrop-saturate-150 sm:px-8 sm:py-4 sm:text-xs">
      <button type="button" class="tracking-[0.3em] text-[10px] text-white sm:text-xs">
        {leftLabel}
      </button>
      <div class="pointer-events-none absolute left-1/2 flex -translate-x-1/2 items-center justify-center text-center">
        <span class="px-4 py-1 text-sm tracking-[0.35em] text-white sm:px-6 sm:py-1.5 sm:text-base">
          {centerLabel}
        </span>
      </div>
      <button type="button" class="tracking-[0.2em] text-[10px] text-white sm:text-xs">
        {rightLabel}
      </button>
    </nav>
  </div>
</header>
